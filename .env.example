# Turso (hosted libSQL) connection. Leave both unset for local dev to fall
# back to a local SQLite file at data/sponsortrack.db.
turso_url=
turso_auth_token=

# Auth.js. Generate with `npx auth secret` or `openssl rand -base64 32`.
AUTH_SECRET=
